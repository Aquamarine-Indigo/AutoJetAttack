import numpy as np


def marshal_action(action):
    # action = np.array([np.random.uniform(0,1), np.random.uniform(0,1), np.random.uniform(0,1), np.random.uniform(0,1)], dtype=np.float64)
    # action = np.array(action, dtype=np.float64)
    # throttlem, pitch, yaw, roll = action[0], action[1], action[2], action[3]
    # control_action = np.array([throttlem, pitch, yaw, roll], dtype=np.float64)

    return action